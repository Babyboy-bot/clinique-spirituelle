# Netlify configuration for Clinique Spirituelle
[build]
  publish = "."
  command = "npm run build"

[dev]
  command = "npm start"
  port = 3001

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200
